# 更新日志

本文档记录人际网络插件的主要功能、行为变化与修复。

格式参考 Keep a Changelog，版本号以 `metadata.yaml` 的历史记录为准。

## [v1.1.2] - 2026-07-31

### 新增

- 新增供外部插件调用的 `upsert_batch_for_plugin` 异步接口，支持批量写入人物、关系和人生经历。
- 接口权限与 LLM 工具相同（可写除管理员备注外的所有字段），人格网络被禁用时静默返回而不抛异常。
- 人物解析支持按 UUID 精确匹配、按姓名或别名模糊匹配，以及通过 `ref` 在同一批次内跨引用新建人物。
- README 补充 `update_personal_network`、`query_personal_network` 两个 LLM 工具的完整参数说明。
- README 将插件联动章节展开为各接口的参数表、返回值说明和代码示例。

## [v1.1.1] - 2026-07-29

### 修复

- 人格关系网生成改为后台任务与状态轮询，避免模型响应时间超过反向代理限制时出现 504。
- 同一人格的进行中任务会被复用，短暂查询失败后可以继续查询；完成结果保留 15 分钟。
- 插件卸载时会取消未完成的生成任务，单个任务等待模型最长 10 分钟。

## [v1.1.0] - 2026-07-29

### 新增

- 关系图支持从人物节点外边框拖向另一人物，以预填主体和目标并创建关系。
- WebUI 支持应用人格设定生成 1-32 个虚拟人物，并选择稀疏、均衡或丰富的关系密度。
- 生成结果提供可编辑、可逐项排除的草稿；无效结果保留原始返回、校验错误和预期 JSON，便于手动修正。
- 关系网生成 Provider 可独立配置；现有人物只允许补全空白资料，不会被改名或覆盖已有资料。
- 人格生成支持最多 2000 字的单次额外要求，关闭生成窗口后自动清空且不写入人物网络。

## [v1.0.0] - 2026-07-28

### 正式版功能

- 按 AstrBot 人格隔离人物卡、平台身份、关系和人生经历。
- 人物卡包含姓名、别名频率、头像、简介、性格、偏好、重要事实和管理员备注。
- 支持真实用户、群友与虚构人物共同参与人格的人际网络，并通过可信平台身份绑定真实发送者。
- 支持任意人物之间的有向多关系、0-100 长期亲密度、关系状态和关系描述。
- 支持多人参与的人生经历、互动活跃度、最近互动及近 7/30/90 天互动统计。
- 提供中文 WebUI，用于查看关系图和人物卡，并管理人物、关系、别名、头像与人生经历。
- 提供 `update_personal_network`、`query_personal_network` LLM 工具及 `/关系查询` 命令。
- 按姓名、别名、可信发送者身份和可配置代词回溯规则注入相关关系上下文。
- 支持配置上下文注入位置、字符与关系数量上限，以及 LLM 主动查询工具开关。
- 支持单人格 JSON 导入导出、冲突预检和 WebP 头像迁移。
- 提供供其他 AstrBot 插件读取结构化快照、关系上下文和幂等记录人生经历的异步接口。
- 支持与 Virtual Life 联动，让虚拟日程参考既有人际关系，并将已完成的多人日程记录为人生经历。
